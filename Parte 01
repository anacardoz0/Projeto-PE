/*             ----Parte 01----
~ Utilizando os conceitos de vetor, implemente um algoritmo que:

01- inclua até 1000 usuários; -feito 
02- edite um usuário;  -feito
03- exclua um usuário; 
04- busque um usuário pelo email;
05- imprima todos os usuários cadastrados; - feito
06- faça backup dos usuários cadastrados;
07- faça restauração dos dados;

~ Dados do usuário:

Id (int) => preenchido automaticamente por números randômicos  (feito)
Nome completo (string)  (feito)
Email (string) => validação do campo: verificar se o caractere "@" aparece  (feito)
Sexo (string) => validação do campo: aceitar somente as palavras Feminino, Masculino e Não declarar   (feito)
Endereço (string)   (feito)
Altura (double) => validação do campo: aceitar valores entre 1 e 2 m.   (feito)
Vacina (tomou a vacina? boolean)    (feito)    */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main (){
	
   int i, id[1000];
   char nome[1000][30], email[1000][30], sexo [1000][15], endereco[1000][30];
   double altura [1000];
   int vacina[1000];
   
   for(i = 0; i < 1000; i++){
 		int resposta;	
		printf("Deseja inserir um usuario 1 (Sim) ou 0 (Nao)? ");
		scanf("%d", &resposta);
		 
		 if(resposta  == 0){
		   	printf("Cadastrados %d\n",i);
		 	break;
		 }
		 
		 /* ^^^ Incluir ^^^ */
		 
  //Id
	for (i = 0; i < 1000; i++)
  {
    printf("%d ", rand() % 1000);
  }
  
  //Nome Completo
    printf("Informe o nome: ");
    fgets(nome[i]30,stdin);
	
 //Email
	printf("Informe o email: ");
	fgets(email[i],30,stdin);
	
	printf("\n%s", email[i]);
		
  printf("%d", strchr(email[i], '@')==0);'
		if(strchr(email[i], '@') == 0){
			printf("Email invalido\n");
			
			printf("Informe o email: ");
	 		fgets(email[i],50,stdin);
		
		}
		else{
			printf("Email valido\n");
		}	 
		 
  //Sexo
  
  printf("Inform o sexo (Feminino, Masculino e Não declarar):");
  fgets(sexo[i],15,stdin);
		
		if(strcmp(sexo[i], "Feminino") == 0 && strcmp(sexo[i], "Masculino") == 0 && strcmp(sexo[i], "Feminino") == 0){
			printf("Sexo invalido\n");
			
			printf("Informe o sexo (Feminino, Masculino e Não declarar):");
	 		fgets(sexo[i],15,stdin);
		}
		else{
			printf("Sexo valido");
		}
  
  //Endereco
  printf("Informe o endereco: ");
	fgets(endereco[i],30,stdin);
	
 //Altura
 
  printf("Informe a altura:");
	scanf("%lf", &altura[i]);
	 	
	printf("%.2lf", altura[i]);
	 	
	if(altura[i] > 1.00 && altura[i] < 2.00){
			printf("Valido\n"); 		
		 }
	 else{
		printf("Altura invalida\n");
		
		printf("Informe a altura:");
		scanf("%lf", &altura[i]);
		 }
 
 //Vacina
 printf("Ja tomou a vacina? 1(Sim)\n 0(Nao)\n");
		 scanf("%d", &vacina[i]);
		 
		 printf("%i");
  
      /* ^^^ Editar ^^^ */
  
  int editar;
	printf("Deseja editar algum usuário 1 (Sim) ou 0 (Nao)? ");
	scanf("%d", &editar);

	if(editar >= 1){
		int opcao, i;
		printf("Qual informacao voce deseja alterar ?\n1: Nome\n2: Email\n3: Sexo\n4: Endereco\n5: Altura\n6: Vacina\n\nDigite o numero: ");
       
		scanf("%d", &opcao);

        switch (opcao)
        {
        case 1:
         printf("Informe o nome:");
	   fgets(nome[i],30,stdin);
	 
            break;
        
        case 2:
         printf("Informe o email:");
	 	fgets(email[i],30,stdin);
			
		printf("\n%s", email[i]);
		
		if(strchr(email[i], '@') == 0){
			printf("Email invalido\n");
			
			printf("Informe o email:");
	 		fgets(email[i],30,stdin);
		
		}
		else{
			printf("Email valido\n");
		}

            break;
        case 3: 
         printf("Inform o sexo (Feminino, Masculino e Não declarar):");
         fgets(sexo[i],15,stdin);
		
		if(strcmp(sexo[i], "Feminino") == 0 && strcmp(sexo[i], "Masculino") == 0 && strcmp(sexo[i], "Feminino") == 0){
			printf("Sexo invalido\n");
			
			printf("Informe o sexo (Feminino, Masculino e Não declarar):");
	 		fgets(sexo[i],15,stdin);
		}
		else{
			printf("Sexo valido");
		}
 
            break;
        
        case 4:
            printf("Informe o endereco:");
	 	    fgets(endereco[index],30,stdin);
            break;

        case 5: 

         printf("Informe a altura:");
	     scanf("%lf", &altura[i]);
	 	
	    printf("%.2lf", altura[i]);
	 	
	    if(altura[i] > 1.00 && altura[i] < 2.00){
			printf("Valido\n"); 		
		 }
	    else{
		printf("Altura invalida\n");
		
		printf("Informe a altura:");
		scanf("%lf", &altura[i]);
		 } 
            break;
        
        case 6:
            printf("Ja tomou a vacina ?  1(Sim)\n 0(Nao)\> ");
		    scanf("%d", &vacina[index]);
		     printf("%i");
           break;

        default:
            printf("\nOpcao invalida\n");
            break;
        } 
        
         /* ^^^ Excluir ^^^ */
         
         
         
          /* ^^^ Buscar ^^^ */
         
         
         
          /* ^^^ Imprimir ^^^ */
    char nome[1000][30] , email[1000][30] , sexo[1000][15] , endereco[1000][30];
 	double altura[1000];
 	int vacina[1000];
	for(i = 0; i < 1000; i++){
		if (vacina[i] == 1)
		{
			printf("Usuario %d:\nNome:%s\nEmail:%s\nsexo:%s\nEndereco:%s\naltura:%.2lf\nVacina:Sim",i,nome[i], email[i], sexo[i] , endereco[i],altura[i]);
		}else if (vacina[i] == 0)
		{
			printf("Usuario %d:\nNome:%s\nEmail:%s\nsexo:%s\nEndereco:%s\naltura:%.2lf\nVacina:Nao",i,nome[i], email[i], sexo[i] , endereco[i],altura[i]);
		}
		
	}


        /* ^^^ Backup ^^^ */
        
        
        /* ^^^ Restaurar ^^^ */
        
        
	}
		 
	return 0;
}
}
